# 实施计划 v0.2 执行记录：Phase 0 + Phase 1 完成

> 计划：`文序-AI原生写作系统-完整实施计划-v0.2.md`（Codex 输出）
> 执行：2026-08-12 ｜ 范围：WEN-001 ~ WEN-011（v0.2 前半：可信可用地基）

## 完成情况

| 任务单 | 内容 | 验证 |
|--------|------|------|
| WEN-001 | Git 基线 + README + 依赖锁定 + 数据库备份 | ✅ 2 commits；workbench-20260812-080126.db 备份；51 测试复现 |
| WEN-002 | 固定资源路径 + `wensu` 入口 + /api/health | ✅ 任意 CWD（D:\）可启动；health ok（+2 测试） |
| WEN-003 | 前端 Vitest/Playwright + 可控 fake transport | ✅ Vitest 7（延迟/乱序/abort/坏JSON/409/422/500）+ Playwright smoke；洞察红测就位 |
| WEN-004 | 迁移框架 + FK/WAL/索引 + version 乐观锁 + 原子修订日志 | ✅ 幂等迁移/失败回滚；409 冲突双份；422 缺版本；AI reason 才写 revision；真实库自动迁移 |
| WEN-005 | typed Block schema 接入 + 旧 ID 兼容 + 稳定 UUID | ✅ 8 类型枚举/ID 约束/长度上限；旧 ID（b1/b<ts>-i）无损；前端创建即 UUID（+11 测试） |
| WEN-006 | autosave 状态机：per-稿状态 + 事件委托 + IndexedDB 恢复 + 409 双份 | ✅ 3 个 E2E 红测转绿：快速切稿不串稿 / Enter 新块仍保存 / in-flight 编辑不丢最新 |
| WEN-007 | 请求序号：迟到响应不覆盖新稿 | ✅ openArticle seq 守卫 + 洞察 seq 校验 |
| WEN-008 | 中文 IME + 最小安全粘贴 | ✅ composition 不拆块；危险 HTML 粘贴只留纯文本（+2 E2E） |
| WEN-009 | 洞察改手动触发 | ✅ 干净打开请求数=0（E2E 断言）；取消/切稿静默 |
| WEN-010 | DOM XSS / URL scheme / Origin+Host 守卫 / CORS 收紧 | ✅ 项目名转义；http/https 白名单；跨源写请求 403；非法 Host 403（+5 测试） |
| WEN-011 | base URL 换 origin 清 Key + scheme 校验 | ✅ 换 host 强制重授权；javascript:/ftp:/明文 http 非本地拒绝（+5 测试） |

## 验证汇总

- **后端：89 passed**（原 51 + 38 新增），1 warning（httpx 弃用警告，依赖升级时处理）
- **前端：Vitest 7 + Playwright E2E 7 = 14 全绿**
- **真实环境实测**（http://127.0.0.1:8766，用户 DeepSeek key）：
  - 打开草稿不自动调模型，显示「生成洞察」按钮 ✓
  - 编辑 → 自动保存 → 状态显示"已保存" ✓
  - 手动洞察 → 真 AI 读文章给出"这段在说/缺什么/3 建议" ✓

## 与计划的偏差（如实记录）

1. **session token cookie 未实现**：改用 **Origin + Host 双重校验**（无 Origin 的同源/非浏览器放行，恶意跨源 403）+ CORS 收紧。语义上满足计划 §5.2 测试 12（跨源网页无法调用写入/AI API），且不破坏现有测试架构。更强力的每进程 token cookie 留到 Phase 8 部署形态冻结后。
2. **safeUrl 未单独 vitest**：位于未拆分的 app.js（WEN-012 ES Modules 拆分后补单测）；由 E2E 搜索链路间接覆盖。
3. **IndexedDB 在 headless Chrome 偶发卡住**：writeRecovery 加 800ms 超时保护（保存主路径不被本地副本阻塞；失败明确提示"本地副本未建立"）。

## 遗留（Phase 2 起）

- ES Modules 拆分（WEN-012）、响应式抽屉（WEN-013/014）、可访问性（WEN-015）
- Playwright 全链路 E2E（WEN-016，当前 7 条覆盖核心风险）
- 引用落库/素材/证据核验（Phase 3-4，WEN-017~021）
- 部署与 token 化（Phase 8）
