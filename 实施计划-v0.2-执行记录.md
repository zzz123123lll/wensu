# 实施计划 v0.2 执行记录：Phase 0 + Phase 1 + Phase 2 全部完成

> 计划：`文序-AI原生写作系统-完整实施计划-v0.2.md`（Codex 输出）
> 执行：2026-08-12 ｜ 范围：WEN-001 ~ WEN-016（v0.2 完整：可信可用 + 可维护 + 窄屏可用）

## 完成情况

| 任务单 | 内容 | 验证 |
|--------|------|------|
| WEN-001 | Git 基线 + README + 依赖锁定 + 数据库备份 | ✅ 2 commits；数据库备份；51 测试复现 |
| WEN-002 | 固定资源路径 + `wensu` 入口 + /api/health | ✅ 任意 CWD 启动；health ok |
| WEN-003 | 前端 Vitest/Playwright + 可控 fake transport | ✅ Vitest 7 + Playwright smoke |
| WEN-004 | 迁移框架 + FK/WAL/索引 + version 乐观锁 + 原子修订日志 | ✅ 幂等迁移/失败回滚；409 双份；422 缺版本；真实库自动迁移 |
| WEN-005 | typed Block schema 接入 + 旧 ID 兼容 + 稳定 UUID | ✅ 类型/ID/长度边界；旧 ID 无损；前端创建即 UUID |
| WEN-006 | autosave 状态机：per-稿状态 + 事件委托 + IndexedDB 恢复 + 409 双份 | ✅ 3 红测转绿：切稿不串/新块保存/in-flight 不丢 |
| WEN-007 | 请求序号：迟到响应不覆盖新稿 | ✅ openArticle/洞察 seq 守卫 |
| WEN-008 | 中文 IME + 最小安全粘贴 | ✅ composition 不拆块；危险 HTML 只留纯文本 |
| WEN-009 | 洞察改手动触发 | ✅ 干净打开请求数=0；取消/切稿静默 |
| WEN-010 | DOM XSS / URL scheme / Origin+Host 守卫 / CORS 收紧 | ✅ 跨源写请求 403；非法 Host 403 |
| WEN-011 | base URL 换 origin 清 Key + scheme 校验 | ✅ 换 host 强制重授权；危险 scheme 拒绝 |
| WEN-012 | ES Modules 拆分（api/state/security/app） | ✅ E2E 7 零回归 |
| WEN-013 | 精确选区锚点 + 就近结果 | ✅ 请求带 UTF-16 选区；响应回显锚点 |
| WEN-014 | 响应式：窄屏互斥抽屉 | ✅ 375/768/1280 三档 E2E；选择草稿自动关抽屉 |
| WEN-015 | 可访问性：键盘/语义/focus-visible | ✅ role=button+tabindex+Enter/Space；dialog aria-modal |
| WEN-016 | 全链路 E2E | ✅ 建项目→建稿→写→AI改写接受→刷新恢复（含新建即打开改进） |

## 最终验证

- **后端：93 passed**（原 51 + 42 新增）
- **前端：Vitest 7 + Playwright E2E 11 = 18 全绿**（覆盖：编辑保存/ID稳定/切稿防串/新块保存/in-flight/IME/粘贴/洞察手动/375/768/1280/全链路）
- **真实环境实测**：打开草稿不自动调模型；编辑→已保存；手动洞察→真 AI 分析
- v0.2 发布承诺（计划 §1.1）：不串稿 ✓ 不丢稿 ✓ 桌面/窄屏可用降级 ✓ 核心链路浏览器回归 ✓

## 与计划的偏差（如实记录）

1. **session token cookie 未实现**：改用 Origin + Host 双重校验（等价 CSRF 防护语义），token 留 Phase 8 部署形态
2. **safeUrl 未单独 vitest**：ES Modules 拆分已完成，后续可补（WEN-012 后模块化支持）
3. **IndexedDB 在 headless Chrome 偶发卡住**：writeRecovery 800ms 超时保护（保存主路径不被阻塞，失败明确提示）
4. **WEN-016 改进**：新建草稿后自动打开（原实现只加载列表不打开）

## 遗留（v0.3 起，Phase 3+）

- Source/Citation/Material 证据数据层（WEN-017/018）
- safe_fetch + prompt-injection 边界（WEN-019）
- 并发搜索 + NDJSON 流式 + 取消 + 缓存（WEN-020）
- 证据型核验（WEN-021）
- 历史/回收站/导出（WEN-022）
- 完整 selection candidate/apply/undo（WEN-023）
- 流式输出、多模型配置、部署（Phase 7-8）
