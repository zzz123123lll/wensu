# 实施计划 v0.2 执行记录：WEN-001 ~ Phase 9 全部完成

> 计划：`文序-AI原生写作系统-完整实施计划-v0.2.md`（Codex 输出）
> 执行：2026-08-12 ｜ 范围：v0.2（WEN-001~016）+ v0.3（WEN-017~021）+ v0.4（WEN-022/023）+ Phase 6-9（智能/记忆/多模型/部署/发布）

## 完成情况

| 任务单 | 内容 | 验证 |
|--------|------|------|
| WEN-001 | Git 基线 + README + 依赖锁定 + 数据库备份 | ✅ 2 commits；数据库备份；51 测试复现 |
| WEN-002 | 固定资源路径 + `wensu` 入口 + /api/health | ✅ 任意 CWD 启动；health ok |
| WEN-003 | 前端 Vitest/Playwright + 可控 fake transport | ✅ Vitest 7 + Playwright smoke |
| WEN-004 | 迁移框架 + FK/WAL/索引 + version 乐观锁 + 原子修订日志 | ✅ 幂等迁移/失败回滚；409 双份；422 缺版本；真实库自动迁移 |
| WEN-005 | typed Block schema 接入 + 旧 ID 兼容 + 稳定 UUID | ✅ 类型/ID/长度边界；旧 ID 无损；前端创建即 UUID |
| WEN-006 | autosave 状态机：per-稿状态 + 事件委托 + IndexedDB 恢复 + 409 双份 | ✅ 3 红测转绿：切稿不串/新块保存/in-flight 不丢 |
| WEN-007 | 请求序号：迟到响应不覆盖新稿 | ✅ openArticle/洞察 seq 守卫 |
| WEN-008 | 中文 IME + 最小安全粘贴 | ✅ composition 不拆块；危险 HTML 只留纯文本 |
| WEN-009 | 洞察改手动触发 | ✅ 干净打开请求数=0；取消/切稿静默 |
| WEN-010 | DOM XSS / URL scheme / Origin+Host 守卫 / CORS 收紧 | ✅ 跨源写请求 403；非法 Host 403 |
| WEN-011 | base URL 换 origin 清 Key + scheme 校验 | ✅ 换 host 强制重授权；危险 scheme 拒绝 |
| WEN-012 | ES Modules 拆分（api/state/security/app） | ✅ E2E 7 零回归 |
| WEN-013 | 精确选区锚点 + 就近结果 | ✅ 请求带 UTF-16 选区；响应回显锚点 |
| WEN-014 | 响应式：窄屏互斥抽屉 | ✅ 375/768/1280 三档 E2E；选择草稿自动关抽屉 |
| WEN-015 | 可访问性：键盘/语义/focus-visible | ✅ role=button+tabindex+Enter/Space；dialog aria-modal |
| WEN-016 | 全链路 E2E | ✅ 建项目→建稿→写→AI改写接受→刷新恢复（含新建即打开改进） |
| WEN-017/018 | 证据数据层：sources/materials/citations 迁移+API+越权+orphaned | ✅ 引用落库+badge渲染+素材存入（同 url 复用；跨项目引用拒绝） |
| WEN-019 | 安全抓取：SSRF/重定向/大小/内容hash → evidence snapshot | ✅ 内网/保留IP拦截；重定向逐跳校验；1MB上限；脚本剥离（+10 测试） |
| WEN-020 | 并发搜索 + NDJSON 流式 + 24h 缓存 | ✅ web/模型并发先到先胜；线程内独立连接（sqlite 跨线程坑已修）；缓存 0.02s 秒回 |
| WEN-021 | 证据型核验 | ✅ 搜索+抓取→快照→LLM 编号引用证据判断；无证据如实"待核实"；前端证据卡 |
| Phase 6 | 写作智能 v0.2（规则优先） | ✅ 信号模型+规则引擎（copilot.py）+块标记菜单+建议卡+限频关闭；无模型可用可解释 |
| Phase 7 | 作者记忆 / Ask 历史 / 多模型 | ✅ prefs 透明可删注入 Ask；草稿隔离历史+checkpoint；model_profiles+task_bindings 独立 Key；连接测试；AI 卡片显模型 |
| Phase 8 | 本地部署加固 | ✅ 127.0.0.1 绑定；随机 session token（HttpOnly）；每日自动备份；wensu 自动开浏览器；诊断包（无 key/正文） |
| Phase 9 | 发布验证与文档闭环 | ✅ docs/ 五篇 + README + 报告书更新；compileall 0 error；发布门证据齐全 |

## 最终验证

- **后端：143 passed**（原 51 + 92 新增）
- **前端：Vitest 7 + Playwright E2E 17 = 24 全绿**
- **Lint：compileall 0 error**
- **真实环境实测**：流式搜索（首次 8-23s 出真结果，缓存 0.02s 秒回）；证据型核验（抓取真实来源为证据，LLM 编号引用判断）；引用/素材落库；规则建议（标记→可解释建议）；Ask 注入偏好与历史；多模型绑定；session token/诊断包/每日备份
- v0.2 发布承诺：不串稿 ✓ 不丢稿 ✓ 桌面/窄屏可用降级 ✓ 核心链路浏览器回归 ✓
- v0.3 交付：引用是真数据、搜索不"卡死"、核验有证据
- Phase 6-9 交付：AI 判断可解释可关闭；记忆透明可删；多模型独立安全；本地部署有备份/恢复/token

## 与计划的偏差（如实记录）

1. **session token cookie 未实现**：改用 Origin + Host 双重校验（等价 CSRF 防护语义），token 留 Phase 8 部署形态
2. **safeUrl 未单独 vitest**：ES Modules 拆分已完成，后续可补（WEN-012 后模块化支持）
3. **IndexedDB 在 headless Chrome 偶发卡住**：writeRecovery 800ms 超时保护（保存主路径不被阻塞，失败明确提示）
4. **WEN-016 改进**：新建草稿后自动打开（原实现只加载列表不打开）

## 遗留（v0.4 之后的可选项）

- 完整 selection candidate/apply/undo 的视觉历史（当前撤销栈已覆盖功能）
- 流式输出（LLM 回复打字机，Ask/改写逐字出现）
- 自动沉淀记忆（多次接受+手动确认后自动写入；当前仅手动保存，符合反模式守卫）
- 云端多用户版（明确不支持，本地版为唯一支持形态）
