# 资产回收地图：文成工作台 → AI 原生写作系统

> 旧产品：D:\微信小程序助手（"文成"微信公众号写作工具台，V7.1.1）
> 结论先行：旧库约 2 万行代码 + 245 个测试。**最值钱的部分恰好是新系统最难造的部分（编辑器、AI 交互、搜索核验），而旧系统最重的部分（界面、流程）恰好是要推翻的。** 它不是失败品，是新系统的半成品仓库。

---

## 一、捡到宝（直接能用，别重写）

### 1. 写作编辑器 —— 中间区的核心，现成的
- **旧模块**：`blocks.py`（后端）+ `static/workbench/editor-blocks.js`（前端）
- **是什么**：文章被切成一块一块（段落/标题/引用/列表/代码/图片/分割线），每块有稳定编号。前后端都实现完了
- **为什么重要**：你要的"富文本 + Block 存储"就是它。还自带防错机制：AI 改完自动同步、旧格式文章自动无损迁移、版本一致性校验
- **改造量**：直接用

### 2. 选中文字 AI 操作 —— 右栏"改写"的后端
- **旧模块**：`selection_ai.py`
- **是什么**：选中文字后，AI 提供六种操作——重写 / 缩短 / 扩写 / 正式 / 口语 / 自定义，返回候选 + 解释
- **对应新产品**：✎ 改写工具。候选 + 解释的形态正好配你定的"段落下方展开候选 → 接受/拒绝"
- **改造量**：小改（展示换成 Diff）

### 3. 智能建议引擎 —— 写作智能的骨架
- **旧模块**：`copilot.py`
- **是什么**：系统自己判断你处于什么阶段（调研/写作/检查/发布），列出此刻最该做的事。每条建议带优先级、定位到具体段落、给可执行动作（去看/采用/忽略）。规则判断不花钱，AI 判断更聪明，没配模型时自动退回规则
- **对应新产品**：右栏"智能推荐位"（你选的 A 手动模式以后升级成 C 推荐模式时的引擎）
- **改造量**：小改（删掉发布类建议，加写作状态判断）

### 4. 搜索 + 抓取 —— 能力层"搜索/阅读"
- **旧模块**：`research.py` + `http_research.py` + `safe_fetch.py`
- **是什么**：不依赖浏览器的联网搜索、抓取网页正文、自带安全防护（禁止访问内网/本地地址）
- **对应新产品**：🔎 搜索工具 + 素材抓取
- **改造量**：小改

### 5. 核验 —— 能力层"检查"
- **旧模块**：`citation_check.py` + `fact_check.py` + `fact_source_check.py`
- **是什么**：引用逐项机械核查（不花钱不联网）、事实核查表、识别"没有来源的断言"
- **对应新产品**：✓ 核验工具（正好支持你定的 可信/存疑/建议修改 三态）
- **改造量**：小改

---

## 二、底座（直接用）

| 旧模块 | 是什么 | 对应新产品 |
|--------|--------|-----------|
| `llm.py` + `providers.py` + `llm_profiles.py` | 统一调用各家模型（云/本地/纯规则三模式），带模型能力档案，解决"模型把预算花在思考上导致空回复"的坑 | 第五层"换模型不推倒产品" |
| `storage.py` | 统一 SQLite 数据库，带快照/检查点/数据迁移 | 全部数据的地基 |
| `projects.py` | 项目→文章 两级模型 | "项目=主题含多篇草稿" |
| `validation.py` | AI 输出先过类型检查才进界面 | "AI 输出先过闸"安全纪律 |
| `credential_store.py` | API Key 用 Windows 系统级加密 | 模型密钥管理 |
| `conversation_service.py` | 聊天超 20 条自动摘要压缩，长聊不爆上下文 | Ask AI 对话区 |
| `memory_service.py` + `workspace_memory.py` | 自动从工作内容提炼要点、去重合并 | 作者上下文雏形 |
| `mcp_server.py` | 标准外部接口，供其他 AI 工具调用 | 能力外露 |
| 245 个测试文件 | 全自动回归测试 | 改代码不心虚 |

---

## 三、能用但要改（按新产品需求收编）

| 旧模块 | 是什么 | 改造方向 |
|--------|--------|---------|
| `revise.py` + `apply_patch.py` | AI 改文章时只动该改的块，其它块编号不变 | 改写/修改工具的核心，适配新产品交互 |
| `humanizer.py` | 检测 AI 痕迹 + 去 AI 味改写 | "表达"类工具之一 |
| `work_history.py` | 自动快照 + 回收站 + 一键恢复 | "修改痕迹"的基础 |
| `style_library.py` + `templates.py` + `recipes.py` | 排版主题、写作模板、写作配方 | 产品化后按需启用 |
| `article_import.py` | 粘贴链接抓整篇文章 | 素材导入 |
| `sensitive_words.py` / `cost.py` / `diagnostics.py` | 敏感词扫描 / 成本估算 / 健康诊断 | 小工具，顺手收编 |

---

## 四、白捡的（如果新产品保留"发布"环节）

微信生态全家桶：公众号多账号管理、公众号排版 HTML 导出、封面图上传压缩、草稿箱推送、发布队列（失败自动重试/定时发布）、发布历史。

第一版不做发布就全部放着不动，哪天要做直接启用。

---

## 五、推倒重做（产品层）

- **旧界面**：`app.py`（1665 行）+ `script.js`（9865 行）——为"流水线式工作流"设计（搜料→生成→三审→修订→导出），跟"作品为中心、人写为主"的新理念冲突
- **三轮评审流程**（内容/合规/编校）：太重，新产品用轻量"核验"替代
- **发布门禁等流程控制**：与"干净、克制"的产品气质不符

---

## 六、总账

| 类别 | 数量 | 说明 |
|------|------|------|
| 直接能用 | ~15 个模块 | 编辑器、改写、建议引擎、搜索、核验、全部底座 |
| 改了能用 | ~8 个 | 补丁、历史、模板、去 AI 味等 |
| 白捡（发布环节） | ~7 个 | 微信全链路 |
| 推倒 | 2 个大文件 + 评审流程 | 界面和流程 |

**一句话**：新系统最难造的三个东西——编辑器、AI 交互判断、搜索核验——旧库全有现成的；旧库最沉的两样——巨型界面和流水线流程——恰好是新系统要推翻的。开发顺序上，旧库可以直接当"后端能力库"用，新产品从零搭壳，把旧模块一个个接进来。
