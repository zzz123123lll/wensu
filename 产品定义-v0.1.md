# AI 原生写作系统 · 产品定义 v0.1

> 状态：设计讨论冻结稿。未决项见文末清单。
> **形态已冻结（2026-08-11）**：三栏布局 + 左栏项目树 + 右栏写作助手面板（非对话）+ iOS 视觉，经原型 v6 与完整走查确认。进入"真实化"阶段（接后端/模型/编辑器）。
> 一句话定位：一个人从"脑子里有点东西"走到"形成一篇真正属于自己的作品"，AI 如何参与——作品是主体，AI 是围绕作品工作的智能层。

---

## 一、五层架构

```
① 作品层 —— 永远的中心（标题、正文、结构、判断、修改痕迹；留下的只有作品）
② 写作智能层 —— 真正要造的：判断作者写作状态，采取合适写作行为（核心黑盒）
③ 能力层 —— AI 的"手"：思考 / 搜索 / 阅读 / 写作 / 检查
④ 写作上下文 —— 作品 / 作者 / 资料 / 讨论 四类上下文
⑤ 基础模型 —— 可替换（GPT / Claude / DeepSeek…），换模型不推倒产品
```

产品原则：
- **外面简单，里面复杂**：功能存在于系统内部，不一定在 UI 里
- **问题发生在哪里，AI 就尽量在哪里解决**
- **人来写作**：AI 是参谋与助手，决定权永远在人

---

## 二、页面布局（已冻结）

```
┌──────────────────────────────────────────────┐
│ 顶栏：项目切换 · 新建 · 设置 · 更多             │
├────────┬─────────────────────────┬──────────┤
│ 侧栏    │ 中间写作区              │ AI工具区  │
│ 可折叠  │ 干净起始页              │ Ask AI   │
│ + 新建  │ 文章标题（大，居中）    │ ✎ 改写    │
│ ▾ 主题A │                        │ 🔎 搜索   │
│   · 草稿1│                        │ ✓ 核验    │
│   · 草稿2│  富文本，正文往下写     │          │
└────────┴─────────────────────────┴──────────┘
```

冻结决定：
| 区域 | 决定 |
|------|------|
| 顶栏 | 横贯最顶部，应用级入口（项目切换/新建/设置） |
| 左栏 | 纯树形：项目（用户自定义）→ 草稿；顶部「新建项目」，可折叠；无分组、无素材分类 |
| 中间 | 富文本（所见即所得），Block 存储；干净起始页，打开不自动加载任何内容 |
| 右栏 | 写作助手面板（非对话）：状态胶囊 + 当前洞察（AI 理解）+ 建议列表（可执行）+ 操作记录 + 低调输入条 + 工具坞（改写/搜索/核验） |
| 打开行为 | 干净页 + 左栏全局列表点选，无"最近项目"默认加载 |
| 项目粒度 | 项目 = 用户自定义（自由命名），项目内多篇草稿；左栏纯树形：项目 → 草稿 |
| 上下文加载 | 惰性：点开草稿才激活该草稿上下文，切换/关闭即释放 |

**重要设计决策（v6 冻结）**：AI 交互**不是对话形态**——用户明确否决"聊天软件感"（iMessage 式气泡 + 大输入框）。右栏定位为"写作智能的窗口"：展示 AI 对当前写作的理解（洞察）、可执行的建议、操作记录；提问输入条降为补充手段。核心原则：AI 的智能要被看见，但人不跟 AI "聊天"。

参考布局：Hermes（左栏全局列表 + 干净主区）+ ChatGPT（可折叠侧栏、干净起始页、顶栏）。

---

## 三、核心交互链路（已冻结）

统一模式：**问题就近解决，结果以 Block 进出文档，决定权在人**。

```
✎ 改写：光标在某段 → 段落下方就地展开候选 + 改动标注 → 接受 / 拒绝
🔎 搜索：光标在某段 → 段落下方展开结果卡片（标题+来源+摘要）
        → "插入引用"（成为 Citation 块）或"存入素材"（进项目素材库）
✓ 核验：选中某句 claim → 句下方展开核验卡片（来源对比）
        → 标注：可信 / 存疑 / 建议修改
💬 Ask AI：右栏对话区 = 草稿区；回答块带「插入到光标处」按钮
        文档 = 唯一正文；AI 永不自动写入，只有用户点插入才进文档
```

---

## 四、写作智能黑盒（机制构想，待深化）

```
感知 → 状态建模 → 行为选择 → 行动(工具) → 返回 → 反馈沉淀
  ↑                                                ↓
  └────── 上下文(作品/作者/资料/讨论) ←──────────────┘
```

- **感知**：显式信号（输入/选中/问话，强）+ 隐式信号（停顿/删改/光标，弱，只做低置信度推断，不主动打扰）
- **状态**：阶段（酝酿/判断/结构/写作/修改/核验）× 问题类型（观点/表达/事实/结构/案例/语气/引用）+ 焦点（光标/选中）
- **行为选择**：规则约束（过滤"此刻可用行为集合"，可枚举可调试）+ 模型选择（生成层才用大模型）。决策与生成解耦 → 换模型只影响生成质量
- **返回**：改写→就近 Diff；资料→段落附近；讨论→对话；结构→拉远；查证→Source 卡片
- **沉淀**：接受/拒绝/放弃的路线写回上下文

核心洞察：同一句话（"我不知道这里怎么写"）在不同状态下行为不同——这是写作智能与聊天机器人的本质区别。

---

## 五、上下文边界（三条存储边界）

| 层级 | 粒度 | 内容 |
|------|------|------|
| 项目级 | 共享 | 资料、来源、素材、讨论记录、放弃的路线 |
| 草稿级 | 独立 | 正文、结构、修改痕迹、草稿专属对话 |
| 用户级 | 跨项目 | 作者偏好：喜欢/讨厌的表达、接受/拒绝的修改类型 |

---

## 六、MVP 范围与建议

- 数据结构按"主题含多草稿"建，**交互主攻单篇闭环**（打开草稿→写→卡住→调工具→写完）
- 右栏用手动工具箱（A 模式）起步，骨架预留智能推荐位（C 模式演进）
- 行为用原子能力（改写/搜索/核验/对话），复合流程由多轮自然组成
- 左栏任务不做状态概念（未完成/已完成），纯列表
- 旧资产回收策略：产品层推倒，能力层回收，数据层择优保留（Block/Source/Material/Citation/Conversation/Memory/Patch/Revision/Project）

---

## 七、视觉风格（已定方向：iOS 式简练有设计）

- **参照**：Apple/iOS 设计语言（SF Pro 系统字体、负字距、紧行高）
- **配色**：近黑 `#1d1d1f` / 浅灰 `#f5f5f7` / 纯白；唯一强调色苹果蓝 `#0071e3`，只用于交互元素
- **层次**：毛玻璃顶栏（backdrop blur），背景色分层替代边框；阴影极少且柔和
- **原则**：内容优先，界面后退；无边框、无纹理、无多余强调色
- **旧库 UI 全弃**：前端全部新建（旧库仅作后端能力库）

---

## 八、未决项 / 假设清单

| # | 问题 | 当前假设 | 状态 |
|---|------|---------|------|
| 1 | 左栏主题分组形式 | 分组头可折叠（未确认，待用户拍板） | 假设 |
| 2 | AI 主动性边界 | 被动为主，轻主动为辅（高置信卡顿信号时提示一次） | 假设 |
| 3 | MVP 感知深度 | 显式信号 + 高置信隐式信号（如选中文字） | 假设 |
| 4 | 目标用户群 | 未定义（中文内容作者？） | 未决 |
| 5 | 编辑器技术实现 | 富文本 + Block 存储，具体栈未定 | 未决 |
| 6 | 发布环节 | 成稿后发布流程（平台/格式）未讨论 | 未决 |
