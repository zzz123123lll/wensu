# 真实化阶段 3 报告：写作助手补全（搜索 + 核验真化）

> 日期：2026-08-11 | 目标：把右栏写作助手的占位功能（搜索/核验）补成真实链路

## 交付内容

| 组件 | 位置 | 说明 |
|------|------|------|
| 搜索服务 | `app/ai_service.py` | 中文 Wikipedia → DuckDuckGo 真检索（零 key，4s 超时）；源不可达降级 LLM 知识线索；结果统一 `{title,url,snippet,source}`，source 区分 `web`（实时检索）/`model`（模型知识） |
| 核验服务 | `app/ai_service.py` | LLM 三态判断（ok 可信 / doubt 存疑 / fix 建议修改）+ 理由 + 可替换表述；坏 JSON 降级 doubt |
| API | `app/main.py` | `POST /api/ai/search`（空查询 400）、`POST /api/ai/check`（LLM 错误 400/502 映射） |
| LLM 客户端 | `app/llm.py` | `chat()` 增加 `temperature` / `max_tokens` 可选参数（向后兼容） |
| 前端 | `web/app.js` + `style.css` | 工具坞搜索/核验接真链路；建议列表 search/check 动作真执行；搜索结果卡（标题+摘要+来源徽章+引用+打开）；核验三态卡（绿/橙/红+采用建议修订）；75s/60s 超时兜底 + 等待文案 |
| 测试 | `tests/`（14 新增） | check 解析 4 + search 降级链 4 + model_search 2 + API 端点 4，共 **51 passed** |

## 验证证据

- `pytest -q` → **51 passed**（37 + 14）
- 真实调用（DeepSeek key）：
  - 搜索「AI 对创作者自主性的影响」→ 3 条模型知识线索（《创造力密码》《创造的心灵》《AI 3.0》），耗时 12~56s（模型波动）
  - 搜索「微信小程序开发」→ 3 条，12.3s
  - 核验「超过半数创作者承认直接采用 AI 段落」→ **doubt**（"来源、样本、定义均未提供，无法核实"）
- 浏览器端到端：搜索卡渲染 3 条 + 「模型知识」徽章 ✓；核验三态卡「存疑」+ 理由 ✓
- 服务运行中：http://127.0.0.1:8766

## 设计要点

- **诚实标注来源**：模型知识降级结果带灰色「模型知识」徽章，真检索带蓝色「已检索」——不把模型记忆冒充实时检索
- **降级链**：真检索（4s×2 快速失败）→ 模型知识 → 空结果友好提示；未配置模型时搜索返回空不报错
- **超时兜底**：前端 AbortController（搜索 75s / 核验 60s），超时给明确文案而非无限转圈
- 搜索源探测失败会消耗 ~8s（wiki+ddg 各 4s 超时），模型调用 12~60s 波动（DeepSeek json_mode 特性）

## 遗留（下一阶段）

- 搜索速度：json_mode 调用不稳定（12~62s），后续可流式输出或换更快模型
- 引用体系：插入的是视觉上标 [N]，来源列表未落库（无 Citation 表）
- 素材库：搜索「存入素材」未做（项目级素材存储待建）
- 流式输出（打字机效果）仍未做
