# 文序 · AI 原生写作系统 项目报告书

日期：2026-08-11 ｜ 版本：v0.1 ｜ 状态：可运行的最小闭环已打通

---

## 一、产品企划愿景

**一句话**：一个人从"脑子里有点东西"写到"一篇真正属于自己的作品"，AI 全程在场，但作品永远是人自己的。

**核心理念**
- 作品是中心——打开软件的感觉是"回来继续写东西"，不是"操作一个 AI 系统"
- 人是作者，AI 是助手——AI 判断你卡在哪，给你最合适的帮助，不替你写
- 外面简单，里面复杂——界面只有写作区 + 一个助手面板，背后是一整套智能系统
- 问题发生在哪里，AI 就在哪里解决——文字问题在文字旁解决，资料问题在段落旁展开

**五层架构**

| 层 | 内容 |
|----|------|
| 作品层 | 永远是中心：标题、正文、想法、引用、修改痕迹 |
| 写作智能层 ★ | 判断"作者处于什么写作状态"，采取合适行为（核心要造的） |
| 能力层 | AI 的"手"：思考 / 搜索 / 阅读 / 写作 / 检查 |
| 上下文层 | 作品 / 作者 / 资料 / 讨论四类上下文 |
| 基础模型 | DeepSeek / GPT / Claude…可换，不推倒产品 |

---

## 二、项目已做

### 1. 想清楚了
- 产品定义 v0.1 冻结（五层架构、布局、交互链路、写作智能机制）
- 旧产品"文成"（2 万行代码）完成资产盘点：14 个模块可直接回收（改写/搜索/核验/多模型适配/Block 模型/加密存储），界面全弃
- 界面经过六轮迭代定稿：左栏用户自定义项目树 + 中间写作区 + 右栏写作助手面板（状态·洞察·建议·工具坞）

### 2. 做出来了（可运行）
- **真存储**：草稿存 SQLite，刷新不丢，自动保存
- **真模型**：右上角 ⚙ 填自己的 API Key（DeepSeek/OpenAI/通义/Kimi 等），本地加密保存
- **五条 AI 链路全部真实可用**：
  - Ask：带着文章上下文真问答
  - 改写：选中文字，AI 真给 2 个方案，可接受/拒绝
  - 洞察：AI 读你的草稿，说"这段在说什么、缺什么"，给可执行建议
  - 搜索：联网检索，源不可达时 AI 给资料线索并诚实标注"模型知识"
  - 核验：AI 三态判断"可信/存疑/建议修改"，可一键采用修订

### 3. 质量
- 后端 143 个测试全过 + 前端 24 个（Vitest 7 + Playwright E2E 17）全过（官方验证 test PASS）
- 全链路降级设计：没配 Key 能提示、模型返回乱码不崩、搜索源失败自动降级

---

## 三、项目未做

| 事项 | 说明 |
|------|------|
| 流式输出 | AI 回复是"一次性"出现，不是打字机 |
| 引用落库 | 正文上标 [1] 只是视觉，来源列表没沉淀 |
| 素材库 | 搜索"存入素材"未做 |
| 选中文字浮层 | 旧原型有，正式版未接入 |
| 多模型并存 | 目前一套配置 |
| 删除/重命名 | 项目、草稿还不能删改 |
| 写作智能状态机 | 概念有了，成系统还早 |
| 作者记忆 | 你的偏好、讨论记录还没沉淀 |
| 部署 | 目前只能本地跑 |
| 前端自动化测试 | 后端 143 测，前端 24 测（单元 7 + E2E 17），全绿 |

---

## 四、项目不足

1. **搜索慢**：模型给资料线索要 12~60 秒，波动大，体验像卡死（有超时提示兜底）
2. **本机网络受限**：Wikipedia 等检索源连不上，"真搜索"实际降级为模型知识——诚实标注了来源，但要等部署环境才算真验证
3. **两条线并存**：UI 实验版和正式版并行，容易混乱（曾因此重复建设）
4. **编辑器单薄**：只有段落/标题/引用，无列表、图片
5. **数据安全感弱**：草稿只在本地库，无导出、无备份、无版本历史

---

## 五、开发者不足

1. **没先盘点就动手**——项目里已有两阶段成果，我没先看，重造了已有的轮子。你在使用中直接感到"越改越不行"。**以后动手前先读目录、读报告、看运行中的服务。**
2. **环境问题反复踩**——路径格式、端口残留这类错误多次犯。已沉淀为经验，但本可更早。
3. **接外部服务不先测连通**——检索源不可达是上线测试才发现。
4. **对模型输出过于乐观**——假设它一定给好东西，实际会空返回，排查多轮。以后一切按"可能坏"设计。
5. **节奏把握差**——你让停的时候还在闷头深挖；技术攻坚没进展应该先向你汇报选择，而不是一直埋头。
6. **前端零测试**——后端有纪律，前端回归全靠手动。（已修复：WEN-003 起建立 Vitest + Playwright 体系，现 24 项覆盖编辑/防串稿/IME/响应式/AI 全链路。）

**下一步建议**：收敛两条线 → 修搜索速度（流式）→ 引用落库。当前版本已可日常写作试用。
