# 文序 · 界面功能讲解（v3 极简原型）

> 原型文件：`prototype/index.html`
> 截图：`C:\Users\HZSM\AppData\Local\hermes\cache\screenshots\browser_screenshot_d60545787c6640689497443683f261cc.png`

## 界面总览

```
┌──────────────────────────────────────────────────────────────┐
│ ① 顶栏：文序 · 项目切换 · 当前文档名 · ＋新建 · ⚙设置          │
├──────────┬─────────────────────────────┬────────────────────┤
│ ② 左栏    │ ③ 中间写作区                 │ ④ 右栏 AI 工具区    │
│ 项目文件   │                             │                    │
│ ＋新建草稿 │  文章标题（大）              │  Ask AI 输入框      │
│ ▾ 产品思考 │  元信息（日期/字数）         │  AI 对话区          │
│   · 草稿1  │  正文（段落/小标题/引用/列表）│  ✎ 改写             │
│   · 草稿2  │                             │  🔎 搜索            │
│ ▾ 公众号   │  （AI 结果卡片就地展开在这）  │  ✓ 核验             │
└──────────┴─────────────────────────────┴────────────────────┘
```

---

## ① 顶栏（横贯最顶部）

| 元素 | 功能 |
|------|------|
| 文序 | 品牌标识 |
| 项目切换（产品思考 ▾） | 切换主题项目。点击展开项目列表 |
| 当前文档名（中间） | 显示正在编辑的草稿名；未打开时显示"选择一篇草稿开始" |
| ＋ 新建 | 新建空白草稿，中间回到干净起始页 |
| ⚙ 设置 | 模型配置（API Key/模型选择）、账号、偏好设置 |

原则：顶栏只放应用级操作，与"写哪篇文章"无关。

---

## ② 左栏：项目文件（侧栏）

| 元素 | 功能 |
|------|------|
| ＋ 新建草稿 | 新建一篇草稿 |
| 分组标题（产品思考 / 公众号长文） | 项目 = 主题，一组草稿归一个主题 |
| 草稿条目 | 点击打开该草稿（中间区加载正文） |
| 蓝色圆点 | 未完成/进行中的草稿 |
| 灰色圆点 | 已完成草稿 |
| 条目右侧时间 | 最后编辑时间 |

只做列表，不做状态管理（未完成/已完成仅用圆点区分，无额外状态系统）。

---

## ③ 中间：写作区（核心，最大区域）

| 元素 | 功能 |
|------|------|
| 干净起始页 | 打开应用/新建时的状态：一句"今天想写点什么？"+ 引导文字。不自动加载任何内容 |
| 文章标题 | 大字号标题，直接点击编辑 |
| 元信息 | 草稿状态 · 最后编辑时间 · 字数 · 待办提示 |
| 正文 | 富文本块：段落 / 小标题 / 引用块 / 列表，所见即所得 |
| 选中文字 → 浮层 | 选中任何文字，头顶浮出黑色工具条：改写 / 搜索 / 核验 |
| AI 结果卡片 | AI 的产出**就地**展开在相关段落下方，不跳走 |

交互核心：问题发生在哪里，AI 就在哪里解决。AI 永远不自动改正文——候选/结果展开后，由你接受或拒绝。

---

## ④ 右栏：AI 工具区

| 元素 | 功能 |
|------|------|
| Ask AI 输入框 | 自由对话。输入任何问题，AI 在下方对话区回答 |
| AI 回复「＋ 插入到光标处」 | 把回答插入正文（绿色高亮显示新增位置） |
| 对话区 | 对话历史，回答可反复参考 |
| ✎ 改写 | 对当前段落/选中文字生成改写候选（重写/缩短/扩写），接受或拒绝 |
| 🔎 搜索 | 联网找来源/案例/数据，结果可"插入引用"或"存入素材" |
| ✓ 核验 | 对选中句做事实与引用检查：可信 / 存疑 / 建议修改 三态 |

右栏是"AI 的操作台"，但**所有改动都要经过你**：AI 产出永远先进右栏或就地卡片，你点接受/插入才进正文。

---

## 三条贯穿原则

1. **人在写**：正文只有你能改，AI 永远只递候选
2. **就地解决**：问题在哪，AI 的结果就出现在哪（段落下方/选区上方/右栏）
3. **以块进出**：AI 内容以 Block 为单位插入/替换，随时可撤销、可重排

## 完整操作示例（一个场景走通）

1. 打开应用 → 干净页 → 左栏点「当 AI 开始替你思考」
2. 读到第二段，觉得论证不够 → 选中那段文字 → 浮层点「搜索」
3. 段落下方展开 3 条搜索结果 → 点「插入引用」，正文出现上标 [1]
4. 觉得开头平淡 → 右栏 Ask AI 问"开头怎么改" → AI 给建议 → 点「插入到光标处」
5. 全文写完 → 选中关键句 → 「核验」→ 看到"存疑" → 点「采用建议」自动修订
6. 关掉应用，下次打开还是干净页，左栏点选继续
