# 真实化阶段 1 报告：真编辑 + 真存储

> 日期：2026-08-11 | 目标：静态原型 → 可用的最小真闭环（SQLite 持久化 + Block 编辑器）

## 交付内容

| 组件 | 位置 | 说明 |
|------|------|------|
| 后端 API | `app/main.py` | FastAPI：项目/草稿/正文块 CRUD（5 个接口） |
| 数据层 | `app/db.py` | SQLite：projects + articles（blocks 存 JSON） |
| 数据模型 | `app/blocks.py` | 旧库回收，零改动 |
| 前端 | `web/` | 三栏形态不变；左栏/中间区真实数据驱动 |
| Block 编辑器 | `web/app.js` | contenteditable：回车分段、空块退格合并、1.2s 防抖自动保存 |
| 测试 | `tests/`（8 个） | db 4 + api 4，全过 |
| 入口 | `main.py` | `uvicorn main:app`（含环境路径清理） |
| 数据文件 | `data/workbench.db` | 重启不丢 |

## 验证证据

- `pytest -q` → **8 passed**（项目 venv 与 hermes verify 双路径均通过）
- `hermes verify` → bootstrap PASS、**test PASS**；readiness 受本机多解释器环境限制（见下）
- 浏览器端到端实测：建项目「随笔」→ 建草稿「第一篇」→ 输入两段 → **刷新页面 → 内容完整恢复**（SQLite 持久化生效）
- 服务运行中：http://127.0.0.1:8766（返回 200）

## 环境事故记录（重要，防再犯）

1. **误装污染**：pip 曾把 fastapi 装进 Hermes 自身 venv（PATH 指向），损坏其 pydantic_core（版本不匹配 2.48.0 vs 需 2.46.4）。已修复：ensurepip + 重装 `pydantic==2.13.4` + `pydantic-core==2.46.4`，Hermes 环境恢复完好。
2. **本机多解释器**：uv 3.11（Hermes 运行时）/ Python 3.12（系统）/ Hermes venv 3.11 并存，bash 会话的 PYTHONPATH 注入 Hermes venv 路径，导致 3.12 解释器加载 3.11 二进制。
   - 防护 1：项目独立 venv（`.venv`，3.12），跑项目一律 `env -u PYTHONPATH ./.venv/Scripts/python.exe`
   - 防护 2：`tests/conftest.py` 剔除 hermes-agent 路径（pytest 环境纯净）
   - 防护 3：`main.py` 顶部同样清理（uvicorn 入口）
3. **hermes verify readiness 无法通过**：verify 的 boot 用 uv 3.11 解释器且无 fastapi——环境层面不可修（不给 uv 3.11 装包）。test 阶段是 verify 核心，已 PASS。

## 遗留

- 右栏 AI 面板为占位（禁用）→ 阶段 2 接真模型
- 编辑器只支持段落/标题/引用块 → 块类型切换、列表、图片留待阶段 2+
- 删除项目/草稿、重命名未做 → 阶段 2
