# 真实化阶段 2 报告：用户自配模型 + 真 AI 链路

> 日期：2026-08-11 | 目标：用户自配 API Key/模型（OpenAI 兼容），打通 Ask/改写/洞察三条真 AI 链路

## 交付内容

| 组件 | 位置 | 说明 |
|------|------|------|
| 设置存储 | `app/settings.py` | base_url/model/api_key；key 用 **Windows DPAPI 加密**落盘（明文不落盘），加密实现可注入 |
| LLM 客户端 | `app/llm.py` | OpenAI 兼容 `chat/completions`，httpx 直调（零额外依赖），错误统一映射：Key 无效/超时/网络/空回复 |
| AI 服务 | `app/ai_service.py` | `ask`（带文章上下文）/ `rewrite`（2 候选，JSON 解析+降级）/ `insight`（洞察+建议，action 校验+降级） |
| API | `app/main.py` | `GET/PUT /api/settings`（GET 永不回传 key）、`POST /api/ai/{ask,rewrite,insight}`（未配置 400 引导，LLM 错误 502） |
| 前端 | `web/` | 设置弹窗（顶栏 ⚙）、右栏解锁（Ask/改写）、洞察自动加载渲染、状态徽章（未配置=橙/已配置=绿+模型名） |

## 用户使用方式（配自己的模型）

1. 打开应用 → 右上角 ⚙
2. 填：API 地址（如 `https://api.deepseek.com/v1`）、模型名（如 `deepseek-chat`）、自己的 API Key
3. 保存 → 右栏状态变绿显示模型名 → 立即可用
4. 支持任意 OpenAI 兼容端点：DeepSeek / OpenAI / 通义 / Kimi / 自定义

## 验证证据

- `pytest -q` → **37 passed**（db 4 / api 4 / settings 6 / settings_api 4 / llm 6 / ai_service 8 / ai_api 5）
- `hermes verify` → bootstrap PASS + **test PASS**
- LLM 层全部用 httpx MockTransport 测试（协议、鉴权头、JSON 模式、401/超时/空回复映射），零真实外部调用
- 浏览器 UI 走查：未配置引导卡 + 橙点徽章 ✓；设置弹窗打开 ✓；假配置保存 → 徽章变绿/弹窗关/toast ✓；测试配置已清理
- 服务运行中：http://127.0.0.1:8766

## 设计要点

- **Key 安全**：DPAPI 加密（Windows 当前用户绑定），GET /api/settings 永不返回 key 明文
- **降级链**：模型输出坏 JSON → 自动降级（改写按行拆、洞察返回空建议），不会崩
- **错误可懂**：用户看到的是"API Key 无效，请在设置中检查"而非堆栈

## 遗留（下一阶段）

- 搜索 / 核验 工具为占位提示（需接真实搜索源 + 引用体系）
- 流式输出（打字机效果）
- 改写候选接受时高亮已实现，Diff 对比（行级改动标注）未做
- 多套配置切换（多模型并存）
