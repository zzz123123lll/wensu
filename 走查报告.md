# 原型走查报告（第一轮）

> 日期：2026-08-11 | 目标：`D:\文序项目\ai-writing-system\prototype\index.html`
> 范围：完整流程走查——建项目 → 写稿 → 建议联动 → 工具链路 → 提问插入 → 状态切换

## 总结

**5 个发现，4 个已修复，1 个记录在案。核心链路全部打通。**

| 严重度 | 数量 | 状态 |
|--------|------|------|
| High | 1 | ✅ 已修复 |
| Medium | 3 | ✅ 已修复（2）/ 原型限制（1） |
| Low | 1 | ✅ 已修复 |
| Info | 1 | 📝 记录 |

## 已验证通过的链路

| 链路 | 结果 |
|------|------|
| 建议「搜索」→ 中间区第二段下方结果卡 | ✅ |
| 搜索「插入引用」→ 正文出现蓝色上标 [N] | ✅（修复后） |
| 建议「核验」→ 三态卡片（可信/存疑/建议修改） | ✅ |
| 核验「采用建议」→ claim 就地修订 + 绿色高亮渐隐 | ✅ |
| 工具坞「改写」→ p1 下方候选卡 | ✅ |
| 改写「接受方案一」→ 正文替换 + 高亮 | ✅ |
| Ask 提问 → 卡片流出现「你问」+「回答」 | ✅ |
| 回答「插入到光标处」→ 正文末尾插入 + 高亮 | ✅ |
| 侧栏折叠/展开 | ✅ |
| 新建项目 → 内联命名浮层 → 项目入列 + 干净页 | ✅（修复后） |
| 切换非示例草稿 → 空白正文占位 | ✅（修复后） |
| 初始 console 零错误 | ✅ |

## 发现与修复

### 1.【High】「插入引用」未写入正文 — 已修复
- **症状**：点「插入引用」只有 toast，正文无 [1] 上标
- **原因**：v6 重构 runSearch 时功能回归（v2 有，v6 丢了）
- **修复**：恢复插入 `<sup>[N]</sup>` 到锚点段落末尾
- **验证**：DOM 确认 p2 尾部出现 `[1]`

### 2.【Medium】系统 prompt 弹窗 — 已修复
- **症状**：新建项目/草稿用 `prompt()`，桌面环境不精致且会阻塞
- **修复**：换成内联命名浮层（毛玻璃卡片 + 输入框 + 确定按钮，Enter 确认 / Esc 取消）
- **验证**：点击后浮层出现，创建"随笔集"成功

### 3.【Medium】切换/新建草稿正文不变 — 已修复（原型层面）
- **症状**：所有草稿共用示例文章内容
- **修复**：非示例草稿显示"空白正文占位"（还没有内容，开始写吧）
- **说明**：真实产品中每个草稿独立存储；原型用占位模拟

### 4.【Low】新建空项目中间区空白 — 已修复
- **症状**：新建项目后既非干净页也非文章，一片空白
- **修复**：回到干净起始页（"今天想写点什么？"）+ 顶栏显示项目名

### 5.【Info】工具 busy 遮罩吞点击 — 记录
- **症状**：工具按钮 busy 动画期间（1.1s）::after 遮罩覆盖，点击无效
- **处理**：原型可接受；真实产品需处理（busy 期间禁用或允许排队）
- **待办**：接真实后端时随交互规范一并解决

## 测试说明

- 浏览器自动化点击 ref 偶有定位偏差（如核验「采用建议」），用 JS 直接触发验证代码本身无误，非产品 bug
- 未测：选区浮层（自动化无法模拟真实文字选中，需人工验证）、多设备响应式
- 视觉模型当前不可用，截图仅供人工确认
